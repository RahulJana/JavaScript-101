const storeManager = {
    firstName: "Rahul",
    lastName: "Jana",
    age: "23",
    job: "Manager"
};


const rahul_profile = `Hi! I'm ${storeManager.firstName} ${storeManager.lastName}, ${storeManager.age} years old. Currently working as a ${storeManager.job}.`

console.log(rahul_profile);


// This is epic.
console.log(`This is a Multi-line string.
Hi! I'm ${storeManager.firstName} ${storeManager.lastName}, ${storeManager.age} years old.
Currently working as a ${storeManager.job}.`);